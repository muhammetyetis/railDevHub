---
interface Props {
	title: string;
	description: string;
	cta?: string;
	link?: string;
}

const { title, description, cta = 'Learn more', link = '#' } = Astro.props as Props;
---

<a href={link} class="block rounded-xl border border-gray-200/60 dark:border-white/10 bg-white dark:bg-gray-800 p-6 shadow-sm hover:shadow-md transition-shadow">
	<h3 class="text-lg font-semibold mb-2">{title}</h3>
	<p class="text-sm text-gray-600 dark:text-gray-300">{description}</p>
	<span class="mt-4 inline-block text-railway-accent text-sm font-medium">{cta} →</span>
</a>



